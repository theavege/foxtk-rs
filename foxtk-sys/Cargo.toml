[package]
name = "foxtk-sys"
version = "0.1.0"
edition = "2024"

[build-dependencies]
cc = { version = "1.2" }
bindgen = { version = "0.72" }
reqwest = { version = "0.12", features = ["json", "native-tls", "blocking"] }
zip-extract = "0.1"
glob = "0.3"
